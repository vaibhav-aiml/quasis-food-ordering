# Dev & test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.3,<9.0
pytest-asyncio>=0.24,<0.25
httpx>=0.27,<0.28        # used by FastAPI's TestClient
ruff>=0.6,<0.7            # linting + formatting
mypy>=1.11,<2.0
